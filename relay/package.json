{
  "name": "enplace-relay",
  "private": true,
  "version": "0.1.0",
  "description": "Enplace cookbook relay: one Durable Object per cookbook speaking the y-websocket protocol.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "wrangler types",
    "typecheck": "npm run types && tsc --noEmit",
    "build": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "partyserver": "^0.5.10",
    "y-partyserver": "^2.2.0",
    "yjs": "^13.6.32"
  }
}
